const fetch = require("node-fetch");
const { ctgpAPI } = require("../../configs/apis");


module.exports = {
    name: "ctgpprofile",
    ownerOnly: false,
    guildOnly: false,
    run: async () => {
        
    }
};